# Sermon Manager

Desktop sermon manager built with Tauri, Node.js, SQLite, and Tailwind.

## Development
- `npm install`
- `npm run dev` (starts UI + API)
- `npm run tauri:dev` (runs Tauri with UI + API)

## Data
- Database file: `data/sermons.db`
- Load Bible CSV later using your existing Python script.
